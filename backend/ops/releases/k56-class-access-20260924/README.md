# Quyền mở bài thi theo lớp và đề khóa 56

**Trạng thái hiện hành:** đã cắt chuyển K56 vào `mapping_db` chung, mở các lớp ERP `on_going` và bật đối soát lượt ERP mới. Các đoạn bên dưới là nhật ký/phương án lịch sử trước cắt chuyển, không còn là hướng dẫn phát hành. Đọc [trạng thái thực tế ngày 24/09/2026](TRANG-THAI-THUC-TE-2026-09-24.md) và kiểm production trước khi thao tác. Bước lịch 04:30 tự chạy và Writing từ bài nộp thật vẫn chưa nghiệm thu.

Trạng thái: **chỉ có trên branch thử, chưa chạy migration hoặc phát hành production**. Đức đã chọn dùng `mapping_db` chung và cập nhật API K67 để cách ly hai khóa. Phương án được kiểm chứng từng bước trong [SHARED-DATABASE-CUTOVER.md](SHARED-DATABASE-CUTOVER.md). Các bước B3–B6 và lệnh backup/compose bên dưới **chỉ áp dụng phương án kho riêng, đã stale đối với hướng được chọn**; không chạy chúng trên `mapping_db`. Cổng cách ly K56/K67 phải đạt trước bất kỳ ghi production nào.

Kiểm production chỉ đọc ngày 24/09: image K56 đang chạy vẫn là `izone-k56-live-results:20260920.1-teacher-session` (ID bắt đầu `2a00b0bf`), `healthy`, restart `0`, database đúng `izone_mapping_k56_ic2264`, ba định nghĩa đề K56 active và **chưa có** bảng quyền lớp–đề. Công cụ `audit_release_state.py` đọc hash source, package/lockfile và phiên bản Node trong container. Hash package/lockfile khác branch, nhưng các phiên bản dependency trực tiếp giống nhau; image mới vẫn phải kiểm đúng package/lockfile của image nền trước khi chép source. **Không** dùng cả branch như một image thay thế hoặc phát hành lớp vá hẹp sáu file. Lệnh kiểm: `python backend/ops/releases/k56-class-access-20260924/audit_release_state.py`.

Phép thử lớp vá hẹp `reconcile_live_gate.py` đã đọc source live vào RAM và áp đúng hai commit cổng quyền `5b11f81`/`25667ca` lên sáu file; cả sáu ghép được, có đủ dấu kiểm cổng quyền và qua `node --check`, **productionWrites=0**. Ba file live có CRLF hoặc context khác branch nên không được áp patch mù theo dòng; công cụ chỉ chọn vị trí khi hunk khớp duy nhất hoặc gần đúng vị trí dự kiến, một import đổi tên duy nhất được xử lý riêng. Ba câu SQL từ lớp vá live còn chạy qua 10 ca PostgreSQL trong RAM: lớp đã mở, lớp chưa mở, lớp thiếu roster, mã Mini tạm và K67 không đổi. Kết quả này **chưa** thay thế kiểm thử hành vi trên image từ source live, backup/restore drill hoặc quyền phát hành. Chạy `python backend/ops/releases/k56-class-access-20260924/reconcile_live_gate.py`; 4 regression fixture riêng kiểm source Git nguyên bản, CRLF, xung đột context và SQL smoke.

Bản stage tạm từ 30 module source K56 live và lớp vá sáu file đã chạy thêm bốn file test backend: 6/7 ca đạt, một ca không đạt về metadata quyền admin của lớp. Source live K56 hiện không tự suy quyền toàn lớp từ `role=admin` và query legacy chưa trả `accessMode`; **không phát hành lớp vá hẹp sáu file**. Bản stage hợp nhất thêm các module branch đã hòa giải trên source live: `stage_live_gate.py --unified-candidate --full-suite --export-context` đạt 203/203 test backend và 10/10 ca SQL trong RAM, 0 skip, 0 ghi production. Dấu SHA-256 cây `src` ứng viên này là `759a22ea355dff58d6497cb1343d12ddc5125c4d9b9afa6037a6d5fa592306ca`; phải trùng ở image sắp build hoặc kiểm lại. Bản đã thử được xuất thành gói build riêng tại `E:/Codex-Data/izone-release-candidates/k56-class-access-ukkq5s_l`, có manifest và bộ kiểm dấu source; **chưa build image, chưa deploy**. `audit_unified_live_drift.py` đối chiếu hash hai profile: 10 module khớp cả K56/K67 live, 1 khớp chỉ K56, 18 khớp chỉ K67 và 8 khác cả hai; phải rà kỹ các module khác biệt trước image release. Chưa có bằng chứng người dùng nhìn thấy quyền admin trên production.

Lệnh đối soát chỉ đọc `python backend/ops/releases/k56-class-access-20260924/bridge_dry_run.py` đã trả nguồn 102 với 29 lớp/447 học viên, cần thêm 28 mapping lớp và 1.305 hàng roster; 36 hàng cũ được giữ. Chưa nhập hàng nào và chưa mở lớp nào.

Lõi nhập roster và bật quyền một chiều đã có trên branch: bản lập hàng mới dùng khóa ERP/đề trong RAM, không tự duyệt Classroom; bản giao dịch khóa bảng và so lại đích trước khi chèn, đọc lại sau khi chèn, lỗi thì rollback. Chỉ sau khi ba roster đủ điều kiện khớp đúng ID và UUID mới có thể bật ba đề trong một giao dịch thứ hai. Kiểm live xác nhận `row_count=510` đếm mọi hàng snapshot, còn 447 hàng `on_going` được chọn; bộ đọc kiểm cả hai số tương ứng. 24 fixture Python và các test PostgreSQL trong full suite đạt; chưa có gói chạy production vì image cổng quyền và backup/readback chưa đạt cổng B3. B6 có cờ `is_eligible`, checkpoint lượt nguồn và giao dịch đối soát local giữ bài cũ mà chặn thi mới; rollback live và nhịp tự động chưa hoàn tất.

Kiểm topology chỉ đọc ngày 24/09: API mapping chung dùng database `mapping_db` qua `mapping-postgres` trên mạng `mapping-api-net`; API K56 dùng database `izone_mapping_k56_ic2264` qua `k56-demo-db` trên mạng `izone-k56-demo_default`. Không có kết nối trực tiếp giữa hai API. Để giữ cô lập, bước nhập roster nên là snapshot một chiều có điều kiện, không đổi `DATABASE_URL` của backend K56 sang database chung và không mở thêm mạng Docker chỉ để đọc roster.

## Người dùng sẽ thấy gì

IC2264 vẫn mở đúng ba đề K56 hiện tại. Theo phạm vi Đức đã chốt, mọi lớp khóa 56 có trạng thái `on_going` trong ERP được mở ba đề Term 1, Term 2 và Mini sau khi đã nhập roster đúng lớp và xác nhận cột Portal. Lớp ngoài phạm vi hoặc thiếu roster vẫn bị đóng. K67 không đổi.

## Điều kiện trước khi phát hành

1. Chụp backup/snapshot database và image hiện hành; xác nhận đúng database K56 riêng `izone_mapping_k56_ic2264` và không có migration cùng tên đã chạy. Nếu lỡ trỏ sang database API chính, dừng trước mọi câu ghi.
2. Đọc lại mapping IC2264 (`erp_course_class_id = 1252`), ba định nghĩa đề K56 active và snapshot K56 từ `mapping_db` dùng chung. Workflow nguồn đã chạy thật: production execution `2332131` và lượt đọc độc lập `2332138` đều thấy 29 lớp/447 đăng ký `on_going`; backend K56 vẫn dùng database riêng và chỉ có mapping/roster IC2264. Nếu snapshot mới khác hoặc có mã lớp–ID mâu thuẫn, dừng để đối soát.
3. Chạy theo thứ tự migration `202609240001_term_test_k56_class_access.sql`, rồi `202609240002_term_test_k56_roster_eligibility.sql` **trước** image backend mới. Migration thứ hai thêm cờ mặc định `true` vào roster hiện có và bảng ghi lượt ERP đã đồng bộ; không sửa bài nộp, UUID hoặc điểm. Backup và kiểm tra migration đã áp dụng trước bước kế tiếp.
4. Dùng quyền API đọc lại đúng ba hàng IC2264 `enabled = true`, không có hàng lớp khác. Nếu không đúng, không đổi image.
5. Chỉ sau khi regression, staging và quality gate chung đạt `ready` mới xin duyệt phát hành backend có cổng quyền; tài liệu này không cấp quyền deploy. Chưa nhập mapping/roster lớp mới trước khi image có cổng quyền chạy và đã thử fail-closed trên lớp chưa mở.

## Mở các lớp K56 đang học

Nguồn quyết định phạm vi là ERP khóa 56, lớp chưa xóa và `status = on_going`; Đức đã xác nhận không cần chờ lịch thi riêng. Workflow `QOeOZVK1gsJ9Wo5r` đã đưa snapshot này vào `mapping_db` dùng chung; nhánh K56 **không lọc lớp theo view Lark**, nhưng vẫn chạy sau chuỗi Lark/Classroom hiện có nên nếu chuỗi cũ dừng thì lượt K56 cũng chưa tới bước ghi. Bước backend kế tiếp phải **đọc snapshot đã đối soát từ database mapping dùng chung**, không nhập tay danh sách mã lớp hoặc đọc lại ERP bằng một logic khác. K56 vẫn giữ database bài thi riêng để không ảnh hưởng K67 và các sản phẩm khác; cần tuyến đồng bộ một chiều có kiểm soát sang các bảng mapping/roster tại đây, với bằng chứng 29 lớp/447 đăng ký, khóa `(class_id, contact_id)` và không làm đổi `student_ref` của bài đã nộp. Chỉ nhận trạng thái học viên `on_going`, xác nhận đủ cột Portal của hai Term trước khi mở bài. Hai lớp IC2322/IC2326 chưa ghép Classroom vì roster rỗng vẫn có snapshot ERP; không tự coi đó là lý do loại khỏi nguồn K56, cũng không tự duyệt Classroom. Sau khi backend có cổng quyền, nhập mapping/roster theo giao dịch và đọc lại số lớp/số học viên; cuối cùng mới bật đúng ba cặp `(test_slug, erp_course_class_id)` cho từng lớp qua giao dịch được phê duyệt. Không bật lớp không còn `on_going` ở thời điểm ghi; không mở 271 lớp lịch sử. Không đưa tên hay ID học viên vào Git/report.

Định danh xuyên tuyến: mỗi học viên có khóa nguồn `(erp_course_class_id, erp_student_contact_id)`, mỗi hàng roster đích thêm `test_slug`; `sync_run_id` là mã lượt đọc, còn khóa chống ghi lặp là `(test_slug, class_id, contact_id)`. Không ghép theo tên hoặc thứ tự dòng. Trước khi ghi, dừng nếu lớp trùng mã nhưng khác ID, học viên sai lớp, nguồn rỗng, số lượng giảm bất thường hoặc thiếu đúng một trong ba đề. Sau khi ghi, đọc lại từng lớp/đề, xác nhận UUID `student_ref` cũ không đổi và mọi hàng mới đúng ID; không tự bật quyền khi phép đối soát chỉ đạt một phần.

Kiểm thử tối thiểu cho tuyến chuyển dữ liệu: một lớp/một học viên; lớp 0 học viên; nhiều lớp xen kẽ và thứ tự đảo; học viên trùng hoặc đổi trạng thái; chạy lại cùng `sync_run_id`; một lớp lỗi giữa lô; lớp cùng tên nhưng ID khác; và đối soát record đích không thuộc phạm vi vẫn nguyên vẹn. Bộ test backend hiện tại đạt 203/203 trên bản ghép source live; không dùng kết quả đó làm bằng chứng tuyến production đã sẵn sàng.

## Lượt ERP tiếp theo — chưa bật tự động

Sau B5, bản xem trước `--audit-eligibility` đếm học viên cần khóa/mở lại và lớp–đề cần bật/tắt, không in danh tính. Nếu có học viên hoặc lớp rời phạm vi, người vận hành phải xem đúng `sync_run_id` và xác nhận riêng lượt đó. `prepare_reconcile_payload` chỉ lập dữ liệu trong RAM khi lớp, ba roster và ba hàng quyền đã có; `eligibility_reconcile.mjs` so lại dữ liệu đích dưới khóa bảng, đổi cờ/quyền và ghi checkpoint cùng một giao dịch. Lượt ERP cũ không được ghi đè lượt mới; lỗi giữa chừng rollback cả ba phần. Kết quả người dùng cần thấy là học viên đã rời không vào bài mới, giảng viên vẫn xem được bài cũ, học viên quay lại có thể vào bài khi lớp còn mở. Hiện mới có regression trên PostgreSQL trong RAM; chưa có lần chạy/rollback/readback live, nên không đưa giao dịch này vào lịch n8n và không coi B6 đã xong.

## Hoàn tác an toàn

Nếu một lớp mới bị mở sai, tắt đúng hàng quyền của lớp–đề đó; không xóa bài hoặc điểm đã tồn tại. **Không rollback riêng image về bản cũ không có cổng quyền khi mapping/roster mới còn trong database**: image cũ có thể mở lại lớp dù hàng quyền đã tắt. Trước phương án rollback image phải chứng minh một tuyến thay thế vẫn chặn lớp chưa duyệt, hoặc cô lập endpoint và xử lý các mapping mới theo backup/giao dịch đã kiểm, có phê duyệt riêng. Đối chiếu bài đang làm và kết quả trước khi chuyển; giữ migration additive để không mất dữ liệu.

## Bằng chứng trên branch

Test database tái hiện lỗi cũ (RED `class_count = 1` cho lớp chưa duyệt), sau sửa GREEN; kiểm cả roster, xác minh học viên, Mini tạm, quyền từng đề, K67 và migration chạy lại. Regression bổ sung: học viên rời lớp giữ bài/UUID nhưng không vào bài mới; K67 chạy với schema không có `is_eligible`. Cần kiểm thêm staging/live readback trước phát hành.
